export enum CustomerCategory {
  EXISTING = 'existing',
  POTENTIAL = 'potential',
}

export enum LeadStatus {
  NEW = 'new',
  FOLLOWUP = 'followup',
  QUALIFIED = 'qualified',
  HOT = 'hot',
  CONVERTED = 'converted',
  LOST = 'lost',
}

export enum LeadSource {
  WEBSITE = 'website',
  REFERRAL = 'referral',
  LINKEDIN = 'linkedin',
  FACEBOOK = 'facebook',
  TWITTER = 'twitter',
  CAMPAIGN = 'campaign',
  INSTAGRAM = 'instagram',
  GENERATED_BY = 'generated_by',
  ON_FIELD = 'on_field',
  OTHER = 'other',
}

export enum Sector {
 
  OTHER = 'other',
}

